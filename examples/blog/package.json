{
  "private": true,
  "name": "blog",
  "version": "0.0.3",
  "scripts": {
    "start": "gatsby develop",
    "build": "gatsby build",
    "now-build": "gatsby build --prefix-paths && mv public dist"
  },
  "dependencies": {
    "gatsby": "latest",
    "gatsby-theme-minimal-blog": "^0.0.3"
  }
}
